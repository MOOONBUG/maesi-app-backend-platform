# RAG 系统启动流程与注意事项

> 适用项目：迈思企业知识库智能问答系统
>
> 项目目录：`D:\数据图表类工具\MySQL数据`
>
> 本文是当前项目的实际操作说明。遇到问题时，按“检查顺序”逐项执行，不要直接重复点击启动脚本。

## 一、目录中几个关键文件

| 文件 | 用途 | 是否可分享 |
|---|---|---|
| `rag_service.py` | FastAPI RAG 后端，负责检索、鉴权、调用大模型和 SSE 流式输出 | 可以分享代码，但先检查是否有硬编码密钥 |
| `index.html` | 前端页面 | 可以分享 |
| `.env` | 非敏感运行配置，如端口、数据库地址、模型名和 API 地址 | 谨慎分享 |
| `.env.secrets` | 数据库密码、API Key、Bearer Token 等敏感配置 | **禁止分享、禁止上传、禁止提交 Git** |
| `start.bat` | Windows 命令行启动脚本 | 可以分享 |
| `start.vbs` | Windows 静默启动脚本 | 可以分享 |
| `acceptance_test.py` | 端到端验收测试 | 可以分享，但测试结果可能含业务摘要 |
| `acceptance_results.json` | 最近一次验收结果 | 谨慎分享，可能包含回答预览 |
| `runtime_backend.out.log` / `runtime_backend.err.log` | 服务运行日志 | 分享前检查是否有请求内容或敏感信息 |
| `README.md` | 项目概览 | 可以分享 |
| `文档注意事项.txt` | 原有数据和运维注意事项 | 已与本文互补，本文优先 |

## 二、首次启动前检查

### 1. 确认 Python

在项目目录打开 PowerShell 或 CMD：

```powershell
python --version
```

建议 Python 3.10 或更高版本。

### 2. 安装依赖

只需首次安装，或依赖发生变化时执行：

```powershell
cd /d "D:\数据图表类工具\MySQL数据"
python -m pip install -r requirements.txt
```

如果电脑使用虚拟环境，应先激活虚拟环境，再执行安装和启动。

### 3. 确认配置文件

必须存在：

```text
.env
.env.secrets
```

`.env` 至少包含以下非敏感配置：

```env
HOST=127.0.0.1
PORT=8000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=rag_app
DB_NAME=ai_knowledge_db
OPENAI_BASE_URL=https://torchai.ai/v1
LLM_MODEL_NAME=claude-sonnet-4-6
```

`.env.secrets` 至少包含以下敏感配置：

```env
DB_PASSWORD=此处填写数据库密码
OPENAI_API_KEY=主 API Key
OPENAI_API_KEYS=主 Key,备用 Key 1,备用 Key 2
BEARER_TOKEN=接口鉴权 Token
```

不要把真实值写进本文档、README、截图或聊天记录。

### 4. 检查敏感文件保护

`.gitignore` 必须包含：

```gitignore
.env
.env.secrets
```

如果 `.env.secrets` 曾经被提交到 Git、上传网盘或发给他人，应立即更换其中的数据库密码、API Key 和 Bearer Token。

## 三、推荐启动流程

### 方案 A：一键启动

双击：

```text
start.vbs
```

它会：

1. 清理占用 8000 端口的旧服务；
2. 从项目目录启动 `rag_service:app`；
3. 自动打开 `index.html`。

如果需要看到后端窗口和错误信息，使用：

```text
start.bat
```

### 方案 B：手动启动，推荐排障时使用

```powershell
cd /d "D:\数据图表类工具\MySQL数据"
python -m uvicorn rag_service:app --host 127.0.0.1 --port 8000
```

不要在正式使用时随意加 `--reload`。开发调试可以使用：

```powershell
python -m uvicorn rag_service:app --host 127.0.0.1 --port 8000 --reload
```

### 方案 C：后台启动并保存日志

```powershell
cd /d "D:\数据图表类工具\MySQL数据"
Start-Process python -ArgumentList '-m','uvicorn','rag_service:app','--host','127.0.0.1','--port','8000' -RedirectStandardOutput 'runtime_backend.out.log' -RedirectStandardError 'runtime_backend.err.log' -WindowStyle Hidden
```

## 四、启动后必须检查

### 1. 健康检查

浏览器打开：

```text
http://127.0.0.1:8000/health
```

正常情况下应看到类似结果：

```json
{
  "status": "running",
  "service": "gpuops-rag-console",
  "llm_configured": true,
  "llm_channel_count": 1
}
```

`llm_configured` 必须为 `true`。如果为 `false`，优先检查 `.env.secrets` 是否存在，以及后端是否从项目目录启动。

### 2. 就绪检查

打开：

```text
http://127.0.0.1:8000/health/ready
```

重点查看：

- `database: true`：数据库连接正常；
- `document_count`：知识库文档数量大于 0；
- `llm_configured: true`：模型配置已加载。

### 3. 前端检查

打开：

```text
file:///D:/数据图表类工具/MySQL数据/index.html
```

或直接双击 `index.html`。输入一个知识库内问题，确认：

- 能返回回答；
- 回答有资料来源；
- 流式内容能够逐步显示；
- 有效问题的结果模式为 `llm`；
- 知识库没有相关资料时应明确拒答，而不是编造。

### 4. 完整验收

在项目目录执行：

```powershell
cd /d "D:\数据图表类工具\MySQL数据"
python acceptance_test.py
```

验收结果写入：

```text
acceptance_results.json
```

当前有效业务用例应满足：

```text
HTTP=200
mode=llm
sources>=1
```

无匹配问题应满足：

```text
HTTP=200
mode=no_match
sources=0
```

## 五、正常关闭和重启

### 查找 8000 端口进程

```powershell
netstat -ano | findstr :8000
```

最后一列是 PID。确认是本项目服务后执行：

```powershell
taskkill /PID <PID> /F
```

然后再启动服务。

不要误杀不属于本项目的进程。

## 六、常见问题处理顺序

### 端口被占用

```powershell
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

再重新启动。

### `llm_configured=false`

按顺序检查：

1. `.env.secrets` 是否位于项目根目录；
2. `OPENAI_API_KEYS` 是否为空；
3. `OPENAI_BASE_URL` 和 `LLM_MODEL_NAME` 是否在 `.env` 中；
4. 是否从正确的项目目录启动；
5. 修改配置后是否真正重启了后端。

### 返回 `grounded_fallback`

这表示模型调用失败，系统使用了有依据的本地降级回答。检查：

1. `rag_runtime.err.log`；
2. API Key 是否过期、额度不足或被撤销；
3. 模型名称是否仍被上游支持；
4. 网络是否能访问 `OPENAI_BASE_URL`；
5. 多 Key 是否都返回 401、429 或 503。

### 返回 `no_match`

这不一定是系统故障。先确认问题是否真的在知识库范围内，再检查：

- 关键词是否过于宽泛；
- 时间范围、指标名称是否写清楚；
- 文档是否已经入库；
- `document_count` 是否大于 0。

### 回答看起来像胡编

不要只看答案正文，要同时看：

- `sources` 是否有来源；
- 来源标题是否与问题相关；
- 回答中的数字是否能在资料中核对；
- 是否明确区分资料事实和分析判断；
- 是否在资料不足时拒答。

如果来源错误，优先修检索；如果来源正确但分析错误，再修提示词、数据口径或模型配置。

## 七、知识库录入规范

1. 单个文本切片建议 300—500 字，保留约 50 字重叠上下文。
2. 复杂表格优先转换为 Markdown 表格或逐行键值结构。
3. 清理页眉、页脚、页码、乱码和重复样式代码。
4. 文档必须带清晰标题、时间范围、数据口径和来源。
5. 财务、销售、库存等数字必须保留单位和计算定义。
6. 涉及部门权限的数据必须添加权限标识，检索时执行权限过滤。

## 八、安全注意事项

- `.env.secrets` 是明文文件，只允许保存在本机受控目录。
- 不要把 API Key、数据库密码、Bearer Token 发到聊天、工单或截图中。
- 不要把 `.env.secrets` 提交到 Git。
- API Key 一旦泄露，应先撤销旧 Key，再配置新 Key。
- Bearer Token 适合本机或内网测试；对外开放前必须增加 HTTPS、限流、来源限制和更严格的用户鉴权。
- 不要把生产数据库密码直接用于测试环境。
- 日志和验收结果可能包含业务文本，分享前先脱敏。
- 更新配置后必须重启后端，运行中的进程不会自动读取新密钥。

## 九、当前项目验收基线

截至本文整理时，系统应达到：

```text
健康检查：通过
Python 语法检查：通过
LLM 通道配置：3 个
有效 RAG 问题：mode=llm
无匹配问题：mode=no_match
模板降级：仅在模型不可用时触发
```

## GPUOps 诊断记录数据库权限

GPU 主机快照是只读采集，不要求数据库写权限；诊断记录接口需要向 `diagnostic_record` 写入数据。若接口返回 `persisted=false` 或列表返回 `degraded=true`，先检查应用账号权限：

```sql
SHOW GRANTS FOR 'rag_app'@'127.0.0.1';
```

由数据库管理员执行 `sql_gpuops_diagnostics.sql` 后，再授予：

```sql
GRANT SELECT, INSERT, UPDATE ON ai_knowledge_db.diagnostic_record TO 'rag_app'@'127.0.0.1';
FLUSH PRIVILEGES;
```

应用不会把“降级未持久化”伪装成成功；权限未补齐时，主服务和 GPU 快照仍可正常运行。
